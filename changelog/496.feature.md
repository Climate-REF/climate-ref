Add CI test to verify solve works without network access (test-offline-solve job).
This test uses pytest-socket to block network access and is expected to fail
until provider lifecycle hooks are implemented.
