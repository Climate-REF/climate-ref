Fixed supplementary dataset selection to pick a single dataset per matching group instead of accumulating multiple candidates across score ties. This eliminates spurious experiment entries (e.g. `1pctCO2`, `esm-1pct-brch-1000PgC`) from appearing alongside expected `historical` and SSP data.
