Mount a ``celeryconfig.py`` via ConfigMap for Flower so ``accept_content`` is read correctly by Celery's config loader. The ``CELERY_ACCEPT_CONTENT`` env var is not picked up by Flower/Kombu directly. The config is user-configurable via ``flower.celeryConfig`` in Helm values.
