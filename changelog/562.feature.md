Added `--limit` flag to the `solve` command to cap the number of executions, and `--dataset-filter` option to both `solve` and `datasets list` commands to filter input datasets by facet values before solving.
