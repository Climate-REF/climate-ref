Added `time_units` and `calendar` metadata to CMIP6 and CMIP7 datasets, enabling proper handling of non-standard CF calendars such as `360_day` and `noleap`. Time values are now stored as `cftime.datetime` objects instead of `datetime.datetime`.
