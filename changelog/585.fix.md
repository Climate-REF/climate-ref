Fixed zero-emission-commitment diagnostic failures caused by space-separated `activity_id` values creating path mismatches, and by incorrect parent timerange computation when datasets are split across multiple files.
