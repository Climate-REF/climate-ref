Consolidated the *In a Nutshell*, *Basic Concepts*, and *Explanation* documentation into a single *Concepts* page, and corrected the documented default dataset parser (the complete parser is the default, not the DRS parser).
