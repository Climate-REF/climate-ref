Minted CMIP7 regression baselines for the ILAMB diagnostics that evaluate against CMIP7 data,
and removed the legacy centralised ILAMB regression fixtures now that the provider is covered entirely by the per-test-case regression framework.
