Collapsed the regression-baseline path placeholders (`<OUTPUT_DIR>` / `<TEST_DATA_DIR>` / `<SOFTWARE_ROOT_DIR>`) into a single `PlaceholderMap` value object shared by the capture and verification paths, replacing the directory parameters previously threaded through both. Committed baselines and their digests are unchanged.
