Corrected command examples in the documentation and package READMEs that used invalid flags (for example `---output-directory` and `--output directory`) or linked to a renamed helper script (`fetch-esgf.py`).
