Added a `--format json` option to `ref datasets list`, `ref providers list`, `ref executions list-groups`, and `ref config list` for machine-readable output with full, untruncated identifiers and paths.
