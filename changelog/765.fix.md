`ref --quiet --help` no longer prints debug log messages to stderr, and successful solves no longer log a spurious `ERROR` when an optional dataset source type (such as CMIP7) is not present in the data catalog.
