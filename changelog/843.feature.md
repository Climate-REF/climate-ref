Added a `RegistryRequest` parser for the ESMValTool reference registry,
so a test case can fetch the reference data an opt-in diagnostic selects.
The fetch and ingest sides now parse through the same function,
so a request cannot name a facet value that ingest would spell differently.
