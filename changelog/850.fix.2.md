Taught Flower to decode the JSON wire format, so task monitoring keeps working after the upgrade.
Flower runs from the stock image and cannot import the REF codec,
so it refused every message with `ContentDisallowed` once tasks stopped being pickled.
A `ref-json` body is plain JSON, so the chart now registers a codec that reads it with the `json` module.
Pickle is no longer accepted there either.
