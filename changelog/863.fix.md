Fixed the `resources_exclusive` flag recorded against each execution.
It was derived only from blocks measured inside the same process,
so a worker in a pool recorded `True` while its siblings were saturating the same container.
Executors now declare whether an execution has the container to itself,
which means `ref executions resources` no longer treats contaminated readings as clean samples.
