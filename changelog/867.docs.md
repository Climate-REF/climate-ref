Rewrote the API surface page so each section lists the symbols a provider may import, with its kind and a one-line description,
rather than describing the module in prose.
The extension points a provider overrides are now listed explicitly for `Diagnostic` and `DiagnosticProvider`.
