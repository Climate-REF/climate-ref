Updated the sea ice sensitivity diagnostic for the recipe changes in ESMValTool v2.15.
The recipe now carries its own model and observational datasets inline, so the REF drops both and
supplies the solved models per variable.
The diagnostic script also renamed its output to `data_values.csv` and gave it a period, regression and
statistic column header, which the metric bundle now reads.
