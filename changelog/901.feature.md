The PMP annual cycle diagnostic now captures the monthly `CalendarMonths` values as series.
Each region and statistic pair produces a 12 point series indexed by month number,
with a `level` dimension for the 3D pressure-level variables.
These values were previously computed by PMP and then discarded.
