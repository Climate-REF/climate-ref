Observational data requirements are now served from `obs4REF` only when they declare it
as a fallback source type.
A custom diagnostic that relied on the old implicit behaviour needs to add
`fallback_source_types=(SourceDatasetType.obs4REF,)` to its requirement.
