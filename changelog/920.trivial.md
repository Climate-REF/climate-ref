Observational data requirements are now served from `obs4REF` only when they declare it
as a fallback source type.
The solver previously folded the whole `obs4REF` catalog into `obs4MIPs` before solving.
