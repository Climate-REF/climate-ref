Pinned the PMP annual cycle and modes of variability CMIP6 requirements to the `Amon` table.
Previously monthly variables from several tables were combined into one execution,
which produced a glob pattern that matched no files.
